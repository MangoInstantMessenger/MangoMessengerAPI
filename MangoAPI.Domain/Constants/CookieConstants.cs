namespace MangoAPI.Domain.Constants
{
    //ToDo Would be better to create a separate project for things like this, for example MangoAPI.Application, but ok for now  
    public static class CookieConstants
    {
        public static string MangoRefreshToken => nameof(MangoRefreshToken);
    }
}